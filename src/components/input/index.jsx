import React from 'react'
import propTypes from 'prop-types'

const Input = ({ name }) => {
  return (
    <div>
      Input
    </div>
  )
}

Input.propTypes = {
  name: propTypes.string
}

export default Input
