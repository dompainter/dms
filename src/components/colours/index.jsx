import React from 'react'
import propTypes from 'prop-types'

const Colours = ({ name }) => {
  return (
    <div>
      Colours
    </div>
  )
}

Colours.propTypes = {
  name: propTypes.string
}

export default Colours
