import React from 'react'
import propTypes from 'prop-types'

const Footer = ({ name }) => {
  return (
    <div>
      Footer
    </div>
  )
}

Footer.propTypes = {
  name: propTypes.string
}

export default Footer
