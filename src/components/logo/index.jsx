import React from 'react'
import propTypes from 'prop-types'

const Logo = ({ name }) => {
  return (
    <div>
      Logo
    </div>
  )
}

Logo.propTypes = {
  name: propTypes.string
}

export default Logo
