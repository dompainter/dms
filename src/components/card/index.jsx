import React from 'react'
import propTypes from 'prop-types'

const Card = ({ name }) => {
  return (
    <div>
      Card
    </div>
  )
}

Card.propTypes = {
  name: propTypes.string
}

export default Card
