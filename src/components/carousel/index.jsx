import React from 'react'
import propTypes from 'prop-types'

const Carousel = ({ name }) => {
  return (
    <div>
      Carousel
    </div>
  )
}

Carousel.propTypes = {
  name: propTypes.string
}

export default Carousel
