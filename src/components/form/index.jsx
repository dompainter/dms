import React from 'react'
import propTypes from 'prop-types'

const Form = ({ name }) => {
  return (
    <div>
      Form
    </div>
  )
}

Form.propTypes = {
  name: propTypes.string
}

export default Form
